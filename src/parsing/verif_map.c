#include "../../inc/cub3D.h"

void	ft_verif_map(char **map)
{
	int	i;
	int	j;

	j = 0;
	i = 0;
	while (map[i])
	{
		while (map[i][j])
		{
			if (map[i][j] == 'N' && map[i][j + 1] == 'O')
				
		}
	}
}